# Leave empty when the backend API is on the same domain (e.g. cPanel with
# Application URL set to "api" → requests to /api/* are proxied by Apache).
# Set to the full origin when the API is on a separate host, e.g.:
#   VITE_API_URL=https://api.bimfalbheritage.com
VITE_API_URL=
