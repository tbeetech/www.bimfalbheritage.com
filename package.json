{
  "name": "bimfalb-heritage",
  "private": true,
  "version": "1.0.0",
  "description": "Bimfalb Heritage - root workspace scripts",
  "scripts": {
    "install-frontend": "cd frontend && npm ci",
    "build": "cd frontend && npm run build",
    "start": "node backend/src/server.js"
  }
}
